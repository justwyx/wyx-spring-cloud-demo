# server
server:
  port: 9000
# spring
spring:
  application:
    name: abcmsc-zuul-depart
# eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
# zuul
zuul:
  # 路由
  routes:
    abcmsc-consumer-depart: /wyx123/**